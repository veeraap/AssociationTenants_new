@page "/"
@namespace Demo_App.Pages
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "_Layout";
}

<link href="css/tailwind.debug.css" rel="stylesheet"   />
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
<link href="Demo_App.styles.css" rel="stylesheet" />
<link href="_content/Syncfusion.Blazor/styles/bootstrap.css" rel="stylesheet">
<script src="_content/Syncfusion.Blazor/scripts/syncfusion-blazor.min.js" type="text/javascript"></script>

<script>
    const rootID = document.getElementById("rootID");
    function toShowHeaderColorPicker() {
        console.log("called");
        const colorPickerHeaderEl = document.getElementById("colorPickerHeader");
        colorPickerHeaderEl.click();
    }

    function toShowContentColorPicker() {
        console.log("called");
        const colorPickerContentEl = document.getElementById("colorPickerContent");
        colorPickerContentEl.click();
    }

    function toShowFooterColorPicker() {
        console.log("called");
        const colorPickerFooterEl = document.getElementById("colorPickerFooter");
        colorPickerFooterEl.click();
    }

    function toShowL1ColorPicker() {
        console.log("called");
        const colorPickerL1El = document.getElementById("colorPicketL1");
        colorPickerL1El.click();
    }


    function toShowL2ColorPicker() {
        console.log("called");
        const colorPickerL2El = document.getElementById("colorPicketL2");
        colorPickerL2El.click();
    }

    function toShowL3ColorPicker() {
        console.log("called");
        const colorPickerL3El = document.getElementById("colorPicketL3");
        colorPickerL3El.click();
    }


    function toShowHeaderFontColorPicker() {
        console.log("called");
        const colorPickerHeaderFontEl = document.getElementById("colorPickerHeaderFont");
        colorPickerHeaderFontEl.click();
    }

    function toShowFooterFontColorPicker() {
        const colorPickerFooterFontEl = document.getElementById("colorPickerFooterFont");
        colorPickerFooterFontEl.click();
    }
   
    // function toDisplay(){

    //     const nextWindowEl = document.getElementById("nextWindow").outerHTML;
    //     const nextWindowEl1 = document.getElementById("nextWindow1").outerHTML;

    //    const cssUrl = '<link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.7/dist/tailwind.min.css" rel="stylesheet"/>';
    //     var scriptElement = document.createElement("script");
    //     var data = 'var setSelectedKey="home";function onClick (key) { setSelectedKey = key; var sections = document.querySelectorAll("#home, #about,#teams"); sections.forEach(function (section) {if (section.id === setSelectedKey) {section.style.display = "block";} else {section.style.display = "none";}});};';
    //   //  scriptElement.innerHTML =
    //     const jsonObj = {
    //         html: nextWindowEl,
    //         css: cssUrl,
    //         javascript: data,
    //         template: nextWindowEl1
    //     }

    //     return jsonObj;
    // }

    function showSpinner() {
        const spinner = document.querySelector('.spinner');
        if (spinner) {
            spinner.classList.add('active');
        }
    }

    function hideSpinner() {
        const spinner = document.querySelector('.spinner');
        if (spinner) {
            spinner.classList.remove('active');
        }
    }

    // window.openNewTab = (base64Html,url) => {
    //     //const htmlContent = atob(base64Html);
    //     var scriptElement = document.createElement("script");
    //     scriptElement.innerHTML = 'var setSelectedKey="home";function onClick (key) { setSelectedKey = key; var sections = document.querySelectorAll("#home, #about,#teams"); sections.forEach(function (section) {if (section.id === setSelectedKey) {section.style.display = "block";} else {section.style.display = "none";}});}; ';
    //     var containerContent = document.getElementById("container");
    //     var newWindowContainer = document.getElementById("contentId");
    //     newWindowContainer.innerHTML="";
    //     // Clone the containerContent element and all its child nodes
    //     var clonedContent = containerContent.cloneNode(true);
    //     // Append the cloned content to the newWindowContainer
    //     newWindowContainer.appendChild(clonedContent);
    //     const cssUrl = '<link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.7/dist/tailwind.min.css" rel="stylesheet">';
    //     const newTab = window.open(url, '_blank');
    //     newTab.location.href = url
    //     const htmlContent = document.getElementById("nextWindow").outerHTML;
    //     newTab.document.body.appendChild(scriptElement);
    //     newTab.document.write(htmlContent);
    //     newTab.document.write(cssUrl);
        
    // };

    

</script>
<component type="typeof(App)" render-mode="ServerPrerendered" />
